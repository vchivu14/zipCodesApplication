package com.zip.zipos.repositories;

import com.zip.zipos.models.ZipCity;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ZipCityRepo extends JpaRepository<ZipCity, Long> {
}
